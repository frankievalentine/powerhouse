---
title: powerhouse
description: Bootstrap AI-native development machines from one curated control plane.
---

`powerhouse` is a CLI-first bootstrap system for macOS and Linux.

It resolves a machine plan from three inputs:

- platform
- profile
- domain

The docs app stays thin on purpose. The real source of truth lives in the workspace registry.

