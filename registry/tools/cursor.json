{
  "id": "cursor",
  "title": "Cursor",
  "description": "The AI-native code editor with built-in agent capabilities.",
  "kind": "ai-cli",
  "priority": 60,
  "supportedPlatforms": ["darwin", "linux", "wsl"],
  "check": {
    "command": "cursor",
    "args": ["--version"]
  },
  "doctorHint": "Cursor is expected by the cursor harness. Install via brew install --cask cursor.",
  "installs": {
    "darwin": [{ "type": "brew", "name": "cursor", "packageType": "cask" }],
    "linux": [{ "type": "brew", "name": "cursor", "packageType": "cask" }]
  }
}
