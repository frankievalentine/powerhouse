{
  "id": "curl",
  "title": "curl",
  "description": "HTTP transfer tool used for bootstrap and script-based installers.",
  "kind": "utility",
  "priority": 5,
  "supportedPlatforms": ["darwin", "linux"],
  "checkCommand": "curl --version",
  "doctorHint": "curl is required for bootstrap and script-driven installs.",
  "installs": {
    "darwin": [{ "type": "brew", "name": "curl" }],
    "linux": [{ "type": "brew", "name": "curl" }]
  }
}

